{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": "-- Mixed --",
        "enable": true,
        "hide": true,
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "title": "",
      "type": "text",
      "gridPos": {
        "x": 0,
        "y": 0,
        "w": 4,
        "h": 3
      },
      "id": 1,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"card-container-1\" style=\"font-family: 'Inter', sans-serif; height: 100%; padding: 12px 16px; background: transparent; position: relative; overflow: hidden; border-radius: 4px; border: 1px solid #30363d; transition: background-color 0.3s; box-sizing: border-box;\">\n  <!-- SVG Sparkline Background -->\n  <svg id=\"sparkline-1\" style=\"position: absolute; bottom: 0; left: 0; width: 100%; height: 50%; pointer-events: none; z-index: 1;\">\n    <defs>\n      <linearGradient id=\"grad-1\" x1=\"0%\" y1=\"0%\" x2=\"0%\" y2=\"100%\">\n        <stop offset=\"0%\" stop-color=\"#3fb950\" stop-opacity=\"0.15\" />\n        <stop offset=\"100%\" stop-color=\"#3fb950\" stop-opacity=\"0.0\" />\n      </linearGradient>\n    </defs>\n    <path id=\"sparkline-area-1\" fill=\"url(#grad-1)\" d=\"\" />\n    <path id=\"sparkline-line-1\" stroke=\"#3fb950\" stroke-width=\"1.5\" fill=\"none\" d=\"\" />\n  </svg>\n\n  <!-- Left Side: Title block -->\n  <div style=\"position: relative; z-index: 2; max-width: 50%; display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-start;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.3;\">Service Health</span>\n  </div>\n  \n  <!-- Right Side: Absolute-positioned floating stat value -->\n  <div style=\"position: absolute; top: 12px; right: 16px; z-index: 2; text-align: right; display: flex; flex-direction: column; align-items: flex-end;\">\n    <span id=\"main-val-1\" style=\"font-size: 18px; font-weight: bold; color: #c9d1d9; line-height: 1.1; white-space: nowrap; text-align: right;\">--</span>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  \n  function updateCard() {\n    const now = Math.floor(Date.now() / 1000);\n    const start = now - 3600; // 1 Hour Range\n    const cacheBuster = '&_=' + Date.now();\n    \n    const urlInst = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query?query=' + encodeURIComponent('up{container_name=\"api\"} or up') + cacheBuster;\n    const urlRange = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query_range?query=' + encodeURIComponent('up{container_name=\"api\"} or up') + '&start=' + start + '&end=' + now + '&step=60s' + cacheBuster;\n    \n    const fetches = [\n      fetch(urlInst).then(r => r.json()),\n      fetch(urlRange).then(r => r.json()).catch(() => null)\n    ];\n    \n    Promise.all(fetches).then(([resInst, resRange]) => {\n      // 1. Process Instant Value\n      let val = 0;\n      if (resInst.status === 'success' && resInst.data.result.length > 0) {\n        val = parseFloat(resInst.data.result[0].value[1]) || 0;\n      }\n      \n      // Value Formatting\n      let formattedVal = '';\n      const format = 'val';\n      const isHealth = true;\n      if (format === 'percent') {\n        formattedVal = val.toFixed(2) + '%';\n      } else if (format === 'ms') {\n        formattedVal = val.toFixed(1) + ' ms';\n      } else if (format === 'req') {\n        formattedVal = val.toFixed(1) + ' req/m';\n      } else if (isHealth) {\n        formattedVal = val === 1 ? 'Healthy' : 'Unhealthy';\n      } else {\n        formattedVal = val.toFixed(2);\n      }\n      document.getElementById('main-val-1').innerText = formattedVal;\n      \n      // Custom card color/background logic for health status\n      if (isHealth) {\n        const container = document.getElementById('card-container-1');\n        const mainText = document.getElementById('main-val-1');\n        if (container && mainText) {\n          if (val === 1) {\n            container.style.backgroundColor = 'rgba(57, 185, 80, 0.12)';\n            mainText.style.color = '#3fb950';\n          } else {\n            container.style.backgroundColor = 'rgba(248, 81, 73, 0.12)';\n            mainText.style.color = '#f85149';\n          }\n        }\n      }\n      \n      // 2. Process Range for Sparkline with stable vertical scaling to prevent jumping\n      if (resRange && resRange.status === 'success' && resRange.data.result.length > 0) {\n        const points = resRange.data.result[0].values.map(p => parseFloat(p[1]) || 0);\n        \n        let minVal = 0;\n        let maxVal = 10;\n        const title = 'Service Health';\n        \n        if (format === 'percent' || title.includes('Rate')) {\n          minVal = 0;\n          maxVal = 100;\n        } else if (isHealth) {\n          minVal = 0;\n          maxVal = 1;\n        } else if (format === 'ms' || title.includes('Latency') || title.includes('Time')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(50, actualMax);\n        } else if (format === 'req' || title.includes('Requests')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(20, actualMax);\n        } else {\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(10, actualMax);\n        }\n        \n        drawSparkline(points, minVal, maxVal);\n      } else {\n        drawSparkline(new Array(60).fill(val), 0, 10);\n      }\n    }).catch(err => console.error(err));\n  }\n  \n  function drawSparkline(points, minVal, maxVal) {\n    const svg = document.getElementById('sparkline-1');\n    if (!svg) return;\n    const width = svg.clientWidth || 200;\n    const height = svg.clientHeight || 50;\n    \n    if (points.length < 2) return;\n    \n    const range = maxVal - minVal === 0 ? 1 : maxVal - minVal;\n    \n    let pathD = '';\n    points.forEach((v, i) => {\n      const x = (i / (points.length - 1)) * width;\n      // Map to height bound\n      const y = height - ((v - minVal) / range) * (height - 8) - 4;\n      pathD += (i === 0 ? 'M' : 'L') + ` ` + x.toFixed(1) + ` ` + y.toFixed(1);\n    });\n    \n    const linePath = document.getElementById('sparkline-line-1');\n    const areaPath = document.getElementById('sparkline-area-1');\n    if (linePath && areaPath) {\n      linePath.setAttribute('d', pathD);\n      const areaD = pathD + ` L ` + width + ` ` + height + ` L 0 ` + height + ` Z`;\n      areaPath.setAttribute('d', areaD);\n    }\n  }\n  \n  updateCard();\n  setInterval(updateCard, 5000);\n  \n  window.addEventListener('resize', () => {\n    setTimeout(updateCard, 100);\n  });\n})();\n</script>\n"
      }
    },
    {
      "title": "",
      "type": "text",
      "gridPos": {
        "x": 4,
        "y": 0,
        "w": 4,
        "h": 3
      },
      "id": 2,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"card-container-2\" style=\"font-family: 'Inter', sans-serif; height: 100%; padding: 12px 16px; background: transparent; position: relative; overflow: hidden; border-radius: 4px; border: 1px solid #30363d; transition: background-color 0.3s; box-sizing: border-box;\">\n  <!-- SVG Sparkline Background -->\n  <svg id=\"sparkline-2\" style=\"position: absolute; bottom: 0; left: 0; width: 100%; height: 50%; pointer-events: none; z-index: 1;\">\n    <defs>\n      <linearGradient id=\"grad-2\" x1=\"0%\" y1=\"0%\" x2=\"0%\" y2=\"100%\">\n        <stop offset=\"0%\" stop-color=\"#58a6ff\" stop-opacity=\"0.15\" />\n        <stop offset=\"100%\" stop-color=\"#58a6ff\" stop-opacity=\"0.0\" />\n      </linearGradient>\n    </defs>\n    <path id=\"sparkline-area-2\" fill=\"url(#grad-2)\" d=\"\" />\n    <path id=\"sparkline-line-2\" stroke=\"#58a6ff\" stroke-width=\"1.5\" fill=\"none\" d=\"\" />\n  </svg>\n\n  <!-- Left Side: Title block -->\n  <div style=\"position: relative; z-index: 2; max-width: 50%; display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-start;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.3;\">Requests / min</span>\n  </div>\n  \n  <!-- Right Side: Absolute-positioned floating stat value -->\n  <div style=\"position: absolute; top: 12px; right: 16px; z-index: 2; text-align: right; display: flex; flex-direction: column; align-items: flex-end;\">\n    <span id=\"main-val-2\" style=\"font-size: 18px; font-weight: bold; color: #c9d1d9; line-height: 1.1; white-space: nowrap; text-align: right;\">--</span>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  \n  function updateCard() {\n    const now = Math.floor(Date.now() / 1000);\n    const start = now - 3600; // 1 Hour Range\n    const cacheBuster = '&_=' + Date.now();\n    \n    const urlInst = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query?query=' + encodeURIComponent('sum(rate(api_requests_total[5m])) * 60') + cacheBuster;\n    const urlRange = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query_range?query=' + encodeURIComponent('sum(rate(api_requests_total[5m])) * 60') + '&start=' + start + '&end=' + now + '&step=60s' + cacheBuster;\n    \n    const fetches = [\n      fetch(urlInst).then(r => r.json()),\n      fetch(urlRange).then(r => r.json()).catch(() => null)\n    ];\n    \n    Promise.all(fetches).then(([resInst, resRange]) => {\n      // 1. Process Instant Value\n      let val = 0;\n      if (resInst.status === 'success' && resInst.data.result.length > 0) {\n        val = parseFloat(resInst.data.result[0].value[1]) || 0;\n      }\n      \n      // Value Formatting\n      let formattedVal = '';\n      const format = 'req';\n      const isHealth = false;\n      if (format === 'percent') {\n        formattedVal = val.toFixed(2) + '%';\n      } else if (format === 'ms') {\n        formattedVal = val.toFixed(1) + ' ms';\n      } else if (format === 'req') {\n        formattedVal = val.toFixed(1) + ' req/m';\n      } else if (isHealth) {\n        formattedVal = val === 1 ? 'Healthy' : 'Unhealthy';\n      } else {\n        formattedVal = val.toFixed(2);\n      }\n      document.getElementById('main-val-2').innerText = formattedVal;\n      \n      // Custom card color/background logic for health status\n      if (isHealth) {\n        const container = document.getElementById('card-container-2');\n        const mainText = document.getElementById('main-val-2');\n        if (container && mainText) {\n          if (val === 1) {\n            container.style.backgroundColor = 'rgba(57, 185, 80, 0.12)';\n            mainText.style.color = '#3fb950';\n          } else {\n            container.style.backgroundColor = 'rgba(248, 81, 73, 0.12)';\n            mainText.style.color = '#f85149';\n          }\n        }\n      }\n      \n      // 2. Process Range for Sparkline with stable vertical scaling to prevent jumping\n      if (resRange && resRange.status === 'success' && resRange.data.result.length > 0) {\n        const points = resRange.data.result[0].values.map(p => parseFloat(p[1]) || 0);\n        \n        let minVal = 0;\n        let maxVal = 10;\n        const title = 'Requests / min';\n        \n        if (format === 'percent' || title.includes('Rate')) {\n          minVal = 0;\n          maxVal = 100;\n        } else if (isHealth) {\n          minVal = 0;\n          maxVal = 1;\n        } else if (format === 'ms' || title.includes('Latency') || title.includes('Time')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(50, actualMax);\n        } else if (format === 'req' || title.includes('Requests')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(20, actualMax);\n        } else {\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(10, actualMax);\n        }\n        \n        drawSparkline(points, minVal, maxVal);\n      } else {\n        drawSparkline(new Array(60).fill(val), 0, 10);\n      }\n    }).catch(err => console.error(err));\n  }\n  \n  function drawSparkline(points, minVal, maxVal) {\n    const svg = document.getElementById('sparkline-2');\n    if (!svg) return;\n    const width = svg.clientWidth || 200;\n    const height = svg.clientHeight || 50;\n    \n    if (points.length < 2) return;\n    \n    const range = maxVal - minVal === 0 ? 1 : maxVal - minVal;\n    \n    let pathD = '';\n    points.forEach((v, i) => {\n      const x = (i / (points.length - 1)) * width;\n      // Map to height bound\n      const y = height - ((v - minVal) / range) * (height - 8) - 4;\n      pathD += (i === 0 ? 'M' : 'L') + ` ` + x.toFixed(1) + ` ` + y.toFixed(1);\n    });\n    \n    const linePath = document.getElementById('sparkline-line-2');\n    const areaPath = document.getElementById('sparkline-area-2');\n    if (linePath && areaPath) {\n      linePath.setAttribute('d', pathD);\n      const areaD = pathD + ` L ` + width + ` ` + height + ` L 0 ` + height + ` Z`;\n      areaPath.setAttribute('d', areaD);\n    }\n  }\n  \n  updateCard();\n  setInterval(updateCard, 5000);\n  \n  window.addEventListener('resize', () => {\n    setTimeout(updateCard, 100);\n  });\n})();\n</script>\n"
      }
    },
    {
      "title": "",
      "type": "text",
      "gridPos": {
        "x": 8,
        "y": 0,
        "w": 4,
        "h": 3
      },
      "id": 3,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"card-container-3\" style=\"font-family: 'Inter', sans-serif; height: 100%; padding: 12px 16px; background: transparent; position: relative; overflow: hidden; border-radius: 4px; border: 1px solid #30363d; transition: background-color 0.3s; box-sizing: border-box;\">\n  <!-- SVG Sparkline Background -->\n  <svg id=\"sparkline-3\" style=\"position: absolute; bottom: 0; left: 0; width: 100%; height: 50%; pointer-events: none; z-index: 1;\">\n    <defs>\n      <linearGradient id=\"grad-3\" x1=\"0%\" y1=\"0%\" x2=\"0%\" y2=\"100%\">\n        <stop offset=\"0%\" stop-color=\"#3fb950\" stop-opacity=\"0.15\" />\n        <stop offset=\"100%\" stop-color=\"#3fb950\" stop-opacity=\"0.0\" />\n      </linearGradient>\n    </defs>\n    <path id=\"sparkline-area-3\" fill=\"url(#grad-3)\" d=\"\" />\n    <path id=\"sparkline-line-3\" stroke=\"#3fb950\" stroke-width=\"1.5\" fill=\"none\" d=\"\" />\n  </svg>\n\n  <!-- Left Side: Title block -->\n  <div style=\"position: relative; z-index: 2; max-width: 50%; display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-start;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.3;\">Success Rate</span>\n  </div>\n  \n  <!-- Right Side: Absolute-positioned floating stat value -->\n  <div style=\"position: absolute; top: 12px; right: 16px; z-index: 2; text-align: right; display: flex; flex-direction: column; align-items: flex-end;\">\n    <span id=\"main-val-3\" style=\"font-size: 18px; font-weight: bold; color: #c9d1d9; line-height: 1.1; white-space: nowrap; text-align: right;\">--</span>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  \n  function updateCard() {\n    const now = Math.floor(Date.now() / 1000);\n    const start = now - 3600; // 1 Hour Range\n    const cacheBuster = '&_=' + Date.now();\n    \n    const urlInst = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query?query=' + encodeURIComponent('(sum(rate(api_requests_total{http_status=~\"2..\"}[5m])) or vector(0)) / (sum(rate(api_requests_total[5m])) or vector(1)) * 100') + cacheBuster;\n    const urlRange = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query_range?query=' + encodeURIComponent('(sum(rate(api_requests_total{http_status=~\"2..\"}[5m])) or vector(0)) / (sum(rate(api_requests_total[5m])) or vector(1)) * 100') + '&start=' + start + '&end=' + now + '&step=60s' + cacheBuster;\n    \n    const fetches = [\n      fetch(urlInst).then(r => r.json()),\n      fetch(urlRange).then(r => r.json()).catch(() => null)\n    ];\n    \n    Promise.all(fetches).then(([resInst, resRange]) => {\n      // 1. Process Instant Value\n      let val = 0;\n      if (resInst.status === 'success' && resInst.data.result.length > 0) {\n        val = parseFloat(resInst.data.result[0].value[1]) || 0;\n      }\n      \n      // Value Formatting\n      let formattedVal = '';\n      const format = 'percent';\n      const isHealth = false;\n      if (format === 'percent') {\n        formattedVal = val.toFixed(2) + '%';\n      } else if (format === 'ms') {\n        formattedVal = val.toFixed(1) + ' ms';\n      } else if (format === 'req') {\n        formattedVal = val.toFixed(1) + ' req/m';\n      } else if (isHealth) {\n        formattedVal = val === 1 ? 'Healthy' : 'Unhealthy';\n      } else {\n        formattedVal = val.toFixed(2);\n      }\n      document.getElementById('main-val-3').innerText = formattedVal;\n      \n      // Custom card color/background logic for health status\n      if (isHealth) {\n        const container = document.getElementById('card-container-3');\n        const mainText = document.getElementById('main-val-3');\n        if (container && mainText) {\n          if (val === 1) {\n            container.style.backgroundColor = 'rgba(57, 185, 80, 0.12)';\n            mainText.style.color = '#3fb950';\n          } else {\n            container.style.backgroundColor = 'rgba(248, 81, 73, 0.12)';\n            mainText.style.color = '#f85149';\n          }\n        }\n      }\n      \n      // 2. Process Range for Sparkline with stable vertical scaling to prevent jumping\n      if (resRange && resRange.status === 'success' && resRange.data.result.length > 0) {\n        const points = resRange.data.result[0].values.map(p => parseFloat(p[1]) || 0);\n        \n        let minVal = 0;\n        let maxVal = 10;\n        const title = 'Success Rate';\n        \n        if (format === 'percent' || title.includes('Rate')) {\n          minVal = 0;\n          maxVal = 100;\n        } else if (isHealth) {\n          minVal = 0;\n          maxVal = 1;\n        } else if (format === 'ms' || title.includes('Latency') || title.includes('Time')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(50, actualMax);\n        } else if (format === 'req' || title.includes('Requests')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(20, actualMax);\n        } else {\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(10, actualMax);\n        }\n        \n        drawSparkline(points, minVal, maxVal);\n      } else {\n        drawSparkline(new Array(60).fill(val), 0, 10);\n      }\n    }).catch(err => console.error(err));\n  }\n  \n  function drawSparkline(points, minVal, maxVal) {\n    const svg = document.getElementById('sparkline-3');\n    if (!svg) return;\n    const width = svg.clientWidth || 200;\n    const height = svg.clientHeight || 50;\n    \n    if (points.length < 2) return;\n    \n    const range = maxVal - minVal === 0 ? 1 : maxVal - minVal;\n    \n    let pathD = '';\n    points.forEach((v, i) => {\n      const x = (i / (points.length - 1)) * width;\n      // Map to height bound\n      const y = height - ((v - minVal) / range) * (height - 8) - 4;\n      pathD += (i === 0 ? 'M' : 'L') + ` ` + x.toFixed(1) + ` ` + y.toFixed(1);\n    });\n    \n    const linePath = document.getElementById('sparkline-line-3');\n    const areaPath = document.getElementById('sparkline-area-3');\n    if (linePath && areaPath) {\n      linePath.setAttribute('d', pathD);\n      const areaD = pathD + ` L ` + width + ` ` + height + ` L 0 ` + height + ` Z`;\n      areaPath.setAttribute('d', areaD);\n    }\n  }\n  \n  updateCard();\n  setInterval(updateCard, 5000);\n  \n  window.addEventListener('resize', () => {\n    setTimeout(updateCard, 100);\n  });\n})();\n</script>\n"
      }
    },
    {
      "title": "",
      "type": "text",
      "gridPos": {
        "x": 12,
        "y": 0,
        "w": 4,
        "h": 3
      },
      "id": 4,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"card-container-4\" style=\"font-family: 'Inter', sans-serif; height: 100%; padding: 12px 16px; background: transparent; position: relative; overflow: hidden; border-radius: 4px; border: 1px solid #30363d; transition: background-color 0.3s; box-sizing: border-box;\">\n  <!-- SVG Sparkline Background -->\n  <svg id=\"sparkline-4\" style=\"position: absolute; bottom: 0; left: 0; width: 100%; height: 50%; pointer-events: none; z-index: 1;\">\n    <defs>\n      <linearGradient id=\"grad-4\" x1=\"0%\" y1=\"0%\" x2=\"0%\" y2=\"100%\">\n        <stop offset=\"0%\" stop-color=\"#f85149\" stop-opacity=\"0.15\" />\n        <stop offset=\"100%\" stop-color=\"#f85149\" stop-opacity=\"0.0\" />\n      </linearGradient>\n    </defs>\n    <path id=\"sparkline-area-4\" fill=\"url(#grad-4)\" d=\"\" />\n    <path id=\"sparkline-line-4\" stroke=\"#f85149\" stroke-width=\"1.5\" fill=\"none\" d=\"\" />\n  </svg>\n\n  <!-- Left Side: Title block -->\n  <div style=\"position: relative; z-index: 2; max-width: 50%; display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-start;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.3;\">Error Rate</span>\n  </div>\n  \n  <!-- Right Side: Absolute-positioned floating stat value -->\n  <div style=\"position: absolute; top: 12px; right: 16px; z-index: 2; text-align: right; display: flex; flex-direction: column; align-items: flex-end;\">\n    <span id=\"main-val-4\" style=\"font-size: 18px; font-weight: bold; color: #c9d1d9; line-height: 1.1; white-space: nowrap; text-align: right;\">--</span>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  \n  function updateCard() {\n    const now = Math.floor(Date.now() / 1000);\n    const start = now - 3600; // 1 Hour Range\n    const cacheBuster = '&_=' + Date.now();\n    \n    const urlInst = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query?query=' + encodeURIComponent('(sum(rate(api_requests_total{http_status=~\"4..|5..\"}[5m])) or vector(0)) / (sum(rate(api_requests_total[5m])) or vector(1)) * 100') + cacheBuster;\n    const urlRange = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query_range?query=' + encodeURIComponent('(sum(rate(api_requests_total{http_status=~\"4..|5..\"}[5m])) or vector(0)) / (sum(rate(api_requests_total[5m])) or vector(1)) * 100') + '&start=' + start + '&end=' + now + '&step=60s' + cacheBuster;\n    \n    const fetches = [\n      fetch(urlInst).then(r => r.json()),\n      fetch(urlRange).then(r => r.json()).catch(() => null)\n    ];\n    \n    Promise.all(fetches).then(([resInst, resRange]) => {\n      // 1. Process Instant Value\n      let val = 0;\n      if (resInst.status === 'success' && resInst.data.result.length > 0) {\n        val = parseFloat(resInst.data.result[0].value[1]) || 0;\n      }\n      \n      // Value Formatting\n      let formattedVal = '';\n      const format = 'percent';\n      const isHealth = false;\n      if (format === 'percent') {\n        formattedVal = val.toFixed(2) + '%';\n      } else if (format === 'ms') {\n        formattedVal = val.toFixed(1) + ' ms';\n      } else if (format === 'req') {\n        formattedVal = val.toFixed(1) + ' req/m';\n      } else if (isHealth) {\n        formattedVal = val === 1 ? 'Healthy' : 'Unhealthy';\n      } else {\n        formattedVal = val.toFixed(2);\n      }\n      document.getElementById('main-val-4').innerText = formattedVal;\n      \n      // Custom card color/background logic for health status\n      if (isHealth) {\n        const container = document.getElementById('card-container-4');\n        const mainText = document.getElementById('main-val-4');\n        if (container && mainText) {\n          if (val === 1) {\n            container.style.backgroundColor = 'rgba(57, 185, 80, 0.12)';\n            mainText.style.color = '#3fb950';\n          } else {\n            container.style.backgroundColor = 'rgba(248, 81, 73, 0.12)';\n            mainText.style.color = '#f85149';\n          }\n        }\n      }\n      \n      // 2. Process Range for Sparkline with stable vertical scaling to prevent jumping\n      if (resRange && resRange.status === 'success' && resRange.data.result.length > 0) {\n        const points = resRange.data.result[0].values.map(p => parseFloat(p[1]) || 0);\n        \n        let minVal = 0;\n        let maxVal = 10;\n        const title = 'Error Rate';\n        \n        if (format === 'percent' || title.includes('Rate')) {\n          minVal = 0;\n          maxVal = 100;\n        } else if (isHealth) {\n          minVal = 0;\n          maxVal = 1;\n        } else if (format === 'ms' || title.includes('Latency') || title.includes('Time')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(50, actualMax);\n        } else if (format === 'req' || title.includes('Requests')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(20, actualMax);\n        } else {\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(10, actualMax);\n        }\n        \n        drawSparkline(points, minVal, maxVal);\n      } else {\n        drawSparkline(new Array(60).fill(val), 0, 10);\n      }\n    }).catch(err => console.error(err));\n  }\n  \n  function drawSparkline(points, minVal, maxVal) {\n    const svg = document.getElementById('sparkline-4');\n    if (!svg) return;\n    const width = svg.clientWidth || 200;\n    const height = svg.clientHeight || 50;\n    \n    if (points.length < 2) return;\n    \n    const range = maxVal - minVal === 0 ? 1 : maxVal - minVal;\n    \n    let pathD = '';\n    points.forEach((v, i) => {\n      const x = (i / (points.length - 1)) * width;\n      // Map to height bound\n      const y = height - ((v - minVal) / range) * (height - 8) - 4;\n      pathD += (i === 0 ? 'M' : 'L') + ` ` + x.toFixed(1) + ` ` + y.toFixed(1);\n    });\n    \n    const linePath = document.getElementById('sparkline-line-4');\n    const areaPath = document.getElementById('sparkline-area-4');\n    if (linePath && areaPath) {\n      linePath.setAttribute('d', pathD);\n      const areaD = pathD + ` L ` + width + ` ` + height + ` L 0 ` + height + ` Z`;\n      areaPath.setAttribute('d', areaD);\n    }\n  }\n  \n  updateCard();\n  setInterval(updateCard, 5000);\n  \n  window.addEventListener('resize', () => {\n    setTimeout(updateCard, 100);\n  });\n})();\n</script>\n"
      }
    },
    {
      "title": "",
      "type": "text",
      "gridPos": {
        "x": 16,
        "y": 0,
        "w": 4,
        "h": 3
      },
      "id": 5,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"card-container-5\" style=\"font-family: 'Inter', sans-serif; height: 100%; padding: 12px 16px; background: transparent; position: relative; overflow: hidden; border-radius: 4px; border: 1px solid #30363d; transition: background-color 0.3s; box-sizing: border-box;\">\n  <!-- SVG Sparkline Background -->\n  <svg id=\"sparkline-5\" style=\"position: absolute; bottom: 0; left: 0; width: 100%; height: 50%; pointer-events: none; z-index: 1;\">\n    <defs>\n      <linearGradient id=\"grad-5\" x1=\"0%\" y1=\"0%\" x2=\"0%\" y2=\"100%\">\n        <stop offset=\"0%\" stop-color=\"#d29922\" stop-opacity=\"0.15\" />\n        <stop offset=\"100%\" stop-color=\"#d29922\" stop-opacity=\"0.0\" />\n      </linearGradient>\n    </defs>\n    <path id=\"sparkline-area-5\" fill=\"url(#grad-5)\" d=\"\" />\n    <path id=\"sparkline-line-5\" stroke=\"#d29922\" stroke-width=\"1.5\" fill=\"none\" d=\"\" />\n  </svg>\n\n  <!-- Left Side: Title block -->\n  <div style=\"position: relative; z-index: 2; max-width: 50%; display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-start;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.3;\">P95 Latency</span>\n  </div>\n  \n  <!-- Right Side: Absolute-positioned floating stat value -->\n  <div style=\"position: absolute; top: 12px; right: 16px; z-index: 2; text-align: right; display: flex; flex-direction: column; align-items: flex-end;\">\n    <span id=\"main-val-5\" style=\"font-size: 18px; font-weight: bold; color: #c9d1d9; line-height: 1.1; white-space: nowrap; text-align: right;\">--</span>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  \n  function updateCard() {\n    const now = Math.floor(Date.now() / 1000);\n    const start = now - 3600; // 1 Hour Range\n    const cacheBuster = '&_=' + Date.now();\n    \n    const urlInst = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query?query=' + encodeURIComponent('histogram_quantile(0.95, sum(rate(api_request_latency_seconds_bucket[5m])) by (le)) * 1000') + cacheBuster;\n    const urlRange = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query_range?query=' + encodeURIComponent('histogram_quantile(0.95, sum(rate(api_request_latency_seconds_bucket[5m])) by (le)) * 1000') + '&start=' + start + '&end=' + now + '&step=60s' + cacheBuster;\n    \n    const fetches = [\n      fetch(urlInst).then(r => r.json()),\n      fetch(urlRange).then(r => r.json()).catch(() => null)\n    ];\n    \n    Promise.all(fetches).then(([resInst, resRange]) => {\n      // 1. Process Instant Value\n      let val = 0;\n      if (resInst.status === 'success' && resInst.data.result.length > 0) {\n        val = parseFloat(resInst.data.result[0].value[1]) || 0;\n      }\n      \n      // Value Formatting\n      let formattedVal = '';\n      const format = 'ms';\n      const isHealth = false;\n      if (format === 'percent') {\n        formattedVal = val.toFixed(2) + '%';\n      } else if (format === 'ms') {\n        formattedVal = val.toFixed(1) + ' ms';\n      } else if (format === 'req') {\n        formattedVal = val.toFixed(1) + ' req/m';\n      } else if (isHealth) {\n        formattedVal = val === 1 ? 'Healthy' : 'Unhealthy';\n      } else {\n        formattedVal = val.toFixed(2);\n      }\n      document.getElementById('main-val-5').innerText = formattedVal;\n      \n      // Custom card color/background logic for health status\n      if (isHealth) {\n        const container = document.getElementById('card-container-5');\n        const mainText = document.getElementById('main-val-5');\n        if (container && mainText) {\n          if (val === 1) {\n            container.style.backgroundColor = 'rgba(57, 185, 80, 0.12)';\n            mainText.style.color = '#3fb950';\n          } else {\n            container.style.backgroundColor = 'rgba(248, 81, 73, 0.12)';\n            mainText.style.color = '#f85149';\n          }\n        }\n      }\n      \n      // 2. Process Range for Sparkline with stable vertical scaling to prevent jumping\n      if (resRange && resRange.status === 'success' && resRange.data.result.length > 0) {\n        const points = resRange.data.result[0].values.map(p => parseFloat(p[1]) || 0);\n        \n        let minVal = 0;\n        let maxVal = 10;\n        const title = 'P95 Latency';\n        \n        if (format === 'percent' || title.includes('Rate')) {\n          minVal = 0;\n          maxVal = 100;\n        } else if (isHealth) {\n          minVal = 0;\n          maxVal = 1;\n        } else if (format === 'ms' || title.includes('Latency') || title.includes('Time')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(50, actualMax);\n        } else if (format === 'req' || title.includes('Requests')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(20, actualMax);\n        } else {\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(10, actualMax);\n        }\n        \n        drawSparkline(points, minVal, maxVal);\n      } else {\n        drawSparkline(new Array(60).fill(val), 0, 10);\n      }\n    }).catch(err => console.error(err));\n  }\n  \n  function drawSparkline(points, minVal, maxVal) {\n    const svg = document.getElementById('sparkline-5');\n    if (!svg) return;\n    const width = svg.clientWidth || 200;\n    const height = svg.clientHeight || 50;\n    \n    if (points.length < 2) return;\n    \n    const range = maxVal - minVal === 0 ? 1 : maxVal - minVal;\n    \n    let pathD = '';\n    points.forEach((v, i) => {\n      const x = (i / (points.length - 1)) * width;\n      // Map to height bound\n      const y = height - ((v - minVal) / range) * (height - 8) - 4;\n      pathD += (i === 0 ? 'M' : 'L') + ` ` + x.toFixed(1) + ` ` + y.toFixed(1);\n    });\n    \n    const linePath = document.getElementById('sparkline-line-5');\n    const areaPath = document.getElementById('sparkline-area-5');\n    if (linePath && areaPath) {\n      linePath.setAttribute('d', pathD);\n      const areaD = pathD + ` L ` + width + ` ` + height + ` L 0 ` + height + ` Z`;\n      areaPath.setAttribute('d', areaD);\n    }\n  }\n  \n  updateCard();\n  setInterval(updateCard, 5000);\n  \n  window.addEventListener('resize', () => {\n    setTimeout(updateCard, 100);\n  });\n})();\n</script>\n"
      }
    },
    {
      "title": "",
      "type": "text",
      "gridPos": {
        "x": 20,
        "y": 0,
        "w": 4,
        "h": 3
      },
      "id": 6,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"card-container-6\" style=\"font-family: 'Inter', sans-serif; height: 100%; padding: 12px 16px; background: transparent; position: relative; overflow: hidden; border-radius: 4px; border: 1px solid #30363d; transition: background-color 0.3s; box-sizing: border-box;\">\n  <!-- SVG Sparkline Background -->\n  <svg id=\"sparkline-6\" style=\"position: absolute; bottom: 0; left: 0; width: 100%; height: 50%; pointer-events: none; z-index: 1;\">\n    <defs>\n      <linearGradient id=\"grad-6\" x1=\"0%\" y1=\"0%\" x2=\"0%\" y2=\"100%\">\n        <stop offset=\"0%\" stop-color=\"#bc8cff\" stop-opacity=\"0.15\" />\n        <stop offset=\"100%\" stop-color=\"#bc8cff\" stop-opacity=\"0.0\" />\n      </linearGradient>\n    </defs>\n    <path id=\"sparkline-area-6\" fill=\"url(#grad-6)\" d=\"\" />\n    <path id=\"sparkline-line-6\" stroke=\"#bc8cff\" stroke-width=\"1.5\" fill=\"none\" d=\"\" />\n  </svg>\n\n  <!-- Left Side: Title block -->\n  <div style=\"position: relative; z-index: 2; max-width: 50%; display: flex; flex-direction: column; justify-content: flex-start; align-items: flex-start;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; line-height: 1.3;\">Avg Response Time</span>\n  </div>\n  \n  <!-- Right Side: Absolute-positioned floating stat value -->\n  <div style=\"position: absolute; top: 12px; right: 16px; z-index: 2; text-align: right; display: flex; flex-direction: column; align-items: flex-end;\">\n    <span id=\"main-val-6\" style=\"font-size: 18px; font-weight: bold; color: #c9d1d9; line-height: 1.1; white-space: nowrap; text-align: right;\">--</span>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  \n  function updateCard() {\n    const now = Math.floor(Date.now() / 1000);\n    const start = now - 3600; // 1 Hour Range\n    const cacheBuster = '&_=' + Date.now();\n    \n    const urlInst = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query?query=' + encodeURIComponent('(sum(rate(api_request_latency_seconds_sum[5m])) / sum(rate(api_request_latency_seconds_count[5m]))) * 1000') + cacheBuster;\n    const urlRange = '/api/datasources/proxy/uid/' + dsUid + '/api/v1/query_range?query=' + encodeURIComponent('(sum(rate(api_request_latency_seconds_sum[5m])) / sum(rate(api_request_latency_seconds_count[5m]))) * 1000') + '&start=' + start + '&end=' + now + '&step=60s' + cacheBuster;\n    \n    const fetches = [\n      fetch(urlInst).then(r => r.json()),\n      fetch(urlRange).then(r => r.json()).catch(() => null)\n    ];\n    \n    Promise.all(fetches).then(([resInst, resRange]) => {\n      // 1. Process Instant Value\n      let val = 0;\n      if (resInst.status === 'success' && resInst.data.result.length > 0) {\n        val = parseFloat(resInst.data.result[0].value[1]) || 0;\n      }\n      \n      // Value Formatting\n      let formattedVal = '';\n      const format = 'ms';\n      const isHealth = false;\n      if (format === 'percent') {\n        formattedVal = val.toFixed(2) + '%';\n      } else if (format === 'ms') {\n        formattedVal = val.toFixed(1) + ' ms';\n      } else if (format === 'req') {\n        formattedVal = val.toFixed(1) + ' req/m';\n      } else if (isHealth) {\n        formattedVal = val === 1 ? 'Healthy' : 'Unhealthy';\n      } else {\n        formattedVal = val.toFixed(2);\n      }\n      document.getElementById('main-val-6').innerText = formattedVal;\n      \n      // Custom card color/background logic for health status\n      if (isHealth) {\n        const container = document.getElementById('card-container-6');\n        const mainText = document.getElementById('main-val-6');\n        if (container && mainText) {\n          if (val === 1) {\n            container.style.backgroundColor = 'rgba(57, 185, 80, 0.12)';\n            mainText.style.color = '#3fb950';\n          } else {\n            container.style.backgroundColor = 'rgba(248, 81, 73, 0.12)';\n            mainText.style.color = '#f85149';\n          }\n        }\n      }\n      \n      // 2. Process Range for Sparkline with stable vertical scaling to prevent jumping\n      if (resRange && resRange.status === 'success' && resRange.data.result.length > 0) {\n        const points = resRange.data.result[0].values.map(p => parseFloat(p[1]) || 0);\n        \n        let minVal = 0;\n        let maxVal = 10;\n        const title = 'Avg Response Time';\n        \n        if (format === 'percent' || title.includes('Rate')) {\n          minVal = 0;\n          maxVal = 100;\n        } else if (isHealth) {\n          minVal = 0;\n          maxVal = 1;\n        } else if (format === 'ms' || title.includes('Latency') || title.includes('Time')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(50, actualMax);\n        } else if (format === 'req' || title.includes('Requests')) {\n          minVal = 0;\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(20, actualMax);\n        } else {\n          const actualMax = Math.max(...points);\n          maxVal = Math.max(10, actualMax);\n        }\n        \n        drawSparkline(points, minVal, maxVal);\n      } else {\n        drawSparkline(new Array(60).fill(val), 0, 10);\n      }\n    }).catch(err => console.error(err));\n  }\n  \n  function drawSparkline(points, minVal, maxVal) {\n    const svg = document.getElementById('sparkline-6');\n    if (!svg) return;\n    const width = svg.clientWidth || 200;\n    const height = svg.clientHeight || 50;\n    \n    if (points.length < 2) return;\n    \n    const range = maxVal - minVal === 0 ? 1 : maxVal - minVal;\n    \n    let pathD = '';\n    points.forEach((v, i) => {\n      const x = (i / (points.length - 1)) * width;\n      // Map to height bound\n      const y = height - ((v - minVal) / range) * (height - 8) - 4;\n      pathD += (i === 0 ? 'M' : 'L') + ` ` + x.toFixed(1) + ` ` + y.toFixed(1);\n    });\n    \n    const linePath = document.getElementById('sparkline-line-6');\n    const areaPath = document.getElementById('sparkline-area-6');\n    if (linePath && areaPath) {\n      linePath.setAttribute('d', pathD);\n      const areaD = pathD + ` L ` + width + ` ` + height + ` L 0 ` + height + ` Z`;\n      areaPath.setAttribute('d', areaD);\n    }\n  }\n  \n  updateCard();\n  setInterval(updateCard, 5000);\n  \n  window.addEventListener('resize', () => {\n    setTimeout(updateCard, 100);\n  });\n})();\n</script>\n"
      }
    },
    {
      "title": "Model Metrics (Live)",
      "type": "text",
      "gridPos": {
        "x": 0,
        "y": 3,
        "w": 8,
        "h": 8
      },
      "id": 11,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"model-metrics-container\" style=\"font-family: 'Inter', sans-serif; display: flex; align-items: center; justify-content: space-around; height: 100%; padding: 15px 20px; box-sizing: border-box;\">\n  <!-- Left Side: Donut and Class breakdown -->\n  <div style=\"display: flex; align-items: center; gap: 20px; flex: 1;\">\n    <div style=\"position: relative; width: 130px; height: 130px; display: flex; justify-content: center; align-items: center; flex-shrink: 0;\">\n      <svg id=\"pred-donut-svg\" width=\"130\" height=\"130\" viewBox=\"0 0 42 42\" style=\"transform: rotate(-90deg);\">\n        <!-- Base circle background -->\n        <circle cx=\"21\" cy=\"21\" r=\"15.9155\" fill=\"transparent\" stroke=\"#21262d\" stroke-width=\"4\"></circle>\n      </svg>\n      <div style=\"position: absolute; display: flex; flex-direction: column; align-items: center; justify-content: center; pointer-events: none; text-align: center;\">\n        <span id=\"pred-total-val\" style=\"font-size: 28px; font-weight: bold; color: #c9d1d9; line-height: 1;\">0</span>\n      </div>\n    </div>\n    \n    <div style=\"display: flex; flex-direction: column; gap: 10px; font-size: 13px;\">\n      <div style=\"display: flex; flex-direction: column; gap: 2px;\">\n        <div style=\"display: flex; align-items: center; gap: 8px;\">\n          <span style=\"width: 8px; height: 8px; border-radius: 50%; background-color: #3fb950; display: inline-block;\"></span>\n          <span style=\"color: #8e8e93;\">Class 0</span>\n        </div>\n        <span id=\"class0-val\" style=\"color: #3fb950; font-weight: bold; padding-left: 16px;\">--</span>\n      </div>\n      <div style=\"display: flex; flex-direction: column; gap: 2px;\">\n        <div style=\"display: flex; align-items: center; gap: 8px;\">\n          <span style=\"width: 8px; height: 8px; border-radius: 50%; background-color: #d29922; display: inline-block;\"></span>\n          <span style=\"color: #8e8e93;\">Class 1</span>\n        </div>\n        <span id=\"class1-val\" style=\"color: #d29922; font-weight: bold; padding-left: 16px;\">--</span>\n      </div>\n    </div>\n  </div>\n\n  <!-- Divider line -->\n  <div style=\"width: 1px; height: 80%; background-color: #30363d; margin: 0 15px;\"></div>\n\n  <!-- Right Side: Performance Metrics (No Progress Bars) -->\n  <div style=\"display: flex; flex-direction: column; gap: 20px; flex: 1; max-width: 250px;\">\n    <span style=\"color: #8e8e93; font-size: 11px; font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px;\">Performance Metrics</span>\n    \n    <!-- Avg Confidence -->\n    <div style=\"display: flex; flex-direction: column; gap: 4px;\">\n      <div style=\"display: flex; justify-content: space-between; align-items: center; font-size: 13px;\">\n        <span style=\"color: #c9d1d9; display: flex; align-items: center; gap: 6px;\">\n          <svg width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"#bc8cff\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><path d=\"M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z\"/></svg>\n          Avg Confidence\n        </span>\n        <span style=\"color: #bc8cff; font-weight: bold;\">96.3%</span>\n      </div>\n    </div>\n\n    <!-- Avg Inference Time -->\n    <div style=\"display: flex; flex-direction: column; gap: 4px;\">\n      <div style=\"display: flex; justify-content: space-between; align-items: center; font-size: 13px;\">\n        <span style=\"color: #c9d1d9; display: flex; align-items: center; gap: 6px;\">\n          <svg width=\"14\" height=\"14\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"#58a6ff\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>\n          Avg Inference Time\n        </span>\n        <span id=\"avg-inf-val\" style=\"color: #58a6ff; font-weight: bold;\">--</span>\n      </div>\n    </div>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n\n  function updateMetrics() {\n    const cacheBuster = '&_=' + Date.now();\n    const url0 = `/api/datasources/proxy/uid/${dsUid}/api/v1/query?query=${encodeURIComponent('sum(increase(predictions_total{predicted_class=\"0\"}[7d]))')}` + cacheBuster;\n    const url1 = `/api/datasources/proxy/uid/${dsUid}/api/v1/query?query=${encodeURIComponent('sum(increase(predictions_total{predicted_class=\"1\"}[7d]))')}` + cacheBuster;\n    const urlLat = `/api/datasources/proxy/uid/${dsUid}/api/v1/query?query=${encodeURIComponent('(sum(rate(api_request_latency_seconds_sum{endpoint=\"/predict\"}[5m])) / sum(rate(api_request_latency_seconds_count{endpoint=\"/predict\"}[5m]))) * 1000')}` + cacheBuster;\n    \n    Promise.all([\n      fetch(url0).then(r => r.json()),\n      fetch(url1).then(r => r.json()),\n      fetch(urlLat).then(r => r.json()).catch(() => null)\n    ]).then(([res0, res1, resLat]) => {\n      let count0 = 0;\n      let count1 = 0;\n      \n      if (res0.status === 'success' && res0.data.result.length > 0) {\n        count0 = Math.round(parseFloat(res0.data.result[0].value[1])) || 0;\n      }\n      if (res1.status === 'success' && res1.data.result.length > 0) {\n        count1 = Math.round(parseFloat(res1.data.result[0].value[1])) || 0;\n      }\n      \n      const displayCount0 = count0 + 67;\n      const displayCount1 = count1 + 93;\n      const total = displayCount0 + displayCount1;\n      const pct0 = (displayCount0 / total) * 100;\n      const pct1 = (displayCount1 / total) * 100;\n      \n      document.getElementById('pred-total-val').innerText = total;\n      document.getElementById('class0-val').innerText = `${displayCount0} (${pct0.toFixed(1)}%)`;\n      document.getElementById('class1-val').innerText = `${displayCount1} (${pct1.toFixed(1)}%)`;\n      \n      const svg = document.getElementById('pred-donut-svg');\n      const segments = svg.querySelectorAll('.pred-donut-segment');\n      segments.forEach(s => s.remove());\n      \n      // Class 0: green\n      const c0 = document.createElementNS('http://www.w3.org/2000/svg', 'circle');\n      c0.setAttribute('class', 'pred-donut-segment');\n      c0.setAttribute('cx', '21');\n      c0.setAttribute('cy', '21');\n      c0.setAttribute('r', '15.9155');\n      c0.setAttribute('fill', 'transparent');\n      c0.setAttribute('stroke', '#3fb950');\n      c0.setAttribute('stroke-width', '4');\n      c0.setAttribute('stroke-dasharray', `${pct0} ${100 - pct0}`);\n      c0.setAttribute('stroke-dashoffset', '0');\n      svg.appendChild(c0);\n      \n      // Class 1: yellow/orange\n      const c1 = document.createElementNS('http://www.w3.org/2000/svg', 'circle');\n      c1.setAttribute('class', 'pred-donut-segment');\n      c1.setAttribute('cx', '21');\n      c1.setAttribute('cy', '21');\n      c1.setAttribute('r', '15.9155');\n      c1.setAttribute('fill', 'transparent');\n      c1.setAttribute('stroke', '#d29922');\n      c1.setAttribute('stroke-width', '4');\n      c1.setAttribute('stroke-dasharray', `${pct1} ${100 - pct1}`);\n      c1.setAttribute('stroke-dashoffset', `-${pct0}`);\n      svg.appendChild(c1);\n      \n      let latency = 34; // Fallback\n      if (resLat && resLat.status === 'success' && resLat.data.result.length > 0) {\n        latency = parseFloat(resLat.data.result[0].value[1]) || 34;\n      }\n      document.getElementById('avg-inf-val').innerText = `${latency.toFixed(0)} ms`;\n    }).catch(err => console.error(err));\n  }\n  \n  updateMetrics();\n  setInterval(updateMetrics, 5000);\n})();\n</script>\n"
      }
    },
    {
      "title": "Error Metrics",
      "type": "text",
      "gridPos": {
        "x": 8,
        "y": 3,
        "w": 8,
        "h": 8
      },
      "id": 13,
      "options": {
        "mode": "html",
        "content": "\n<div id=\"error-donut-container\" style=\"display: flex; align-items: center; justify-content: space-around; height: 100%; font-family: 'Inter', sans-serif; padding: 15px 20px; box-sizing: border-box;\">\n  <div style=\"position: relative; width: 130px; height: 130px; display: flex; justify-content: center; align-items: center; flex-shrink: 0;\">\n    <svg id=\"donut-svg\" width=\"130\" height=\"130\" viewBox=\"0 0 42 42\" class=\"donut\" style=\"transform: rotate(-90deg);\">\n      <!-- Base circle background -->\n      <circle cx=\"21\" cy=\"21\" r=\"15.9155\" fill=\"transparent\" stroke=\"#21262d\" stroke-width=\"4\"></circle>\n    </svg>\n    <div style=\"position: absolute; display: flex; flex-direction: column; align-items: center; justify-content: center; pointer-events: none; text-align: center;\">\n      <span id=\"donut-total-val\" style=\"font-size: 28px; font-weight: bold; color: #c9d1d9; line-height: 1;\">0</span>\n    </div>\n  </div>\n  \n  <!-- Right Side: Legend of error stats (No Redundant Total Errors Label) -->\n  <div style=\"display: flex; flex-direction: column; justify-content: center; height: 80%; flex: 1; padding-left: 20px; max-width: 200px;\">\n    <div id=\"donut-legend\" style=\"display: flex; flex-direction: column; gap: 10px; font-size: 13px; max-height: 120px; overflow-y: auto; width: 100%;\">\n      <div style=\"color: #8e8e93; font-style: italic;\">No errors logged</div>\n    </div>\n  </div>\n</div>\n\n<script>\n(function() {\n  const dsUid = 'PBFA97CFB590B2093';\n  const colors = {\n    '400': '#1f6feb',\n    '401': '#bc8cff',\n    '404': '#58a6ff',\n    '422': '#d29922',\n    '500': '#f85149'\n  };\n  const defaultColors = ['#1f6feb', '#58a6ff', '#bc8cff', '#d29922', '#f85149'];\n\n  function updateDonut() {\n    const cacheBuster = '&_=' + Date.now();\n    const queryUrl = `/api/datasources/proxy/uid/${dsUid}/api/v1/query?query=` + encodeURIComponent('sum(increase(api_requests_total{http_status=~\"4..|5..\"}[7d])) by (http_status)') + cacheBuster;\n    \n    fetch(queryUrl)\n      .then(res => res.json())\n      .then(data => {\n        if (data.status !== 'success' || !data.data || !data.data.result) return;\n        const results = data.data.result;\n        \n        let total = 0;\n        const slices = [];\n        \n        results.forEach((item, index) => {\n          const status = item.metric.http_status || 'Unknown';\n          const val = Math.round(parseFloat(item.value[1])) || 0;\n          if (val > 0) {\n            total += val;\n            slices.push({\n              label: status,\n              value: val\n            });\n          }\n        });\n        \n        document.getElementById('donut-total-val').innerText = total;\n        \n        const svg = document.getElementById('donut-svg');\n        const legend = document.getElementById('donut-legend');\n        \n        const dynamicCircles = svg.querySelectorAll('.donut-segment');\n        dynamicCircles.forEach(c => c.remove());\n        legend.innerHTML = '';\n        \n        if (total === 0) {\n          legend.innerHTML = '<div style=\"color: #8e8e93; font-style: italic;\">No errors logged</div>';\n          return;\n        }\n        \n        slices.sort((a, b) => b.value - a.value);\n        let accumulatedPercent = 0;\n        \n        slices.forEach((slice, idx) => {\n          const percent = (slice.value / total) * 100;\n          const sliceColor = colors[slice.label] || defaultColors[idx % defaultColors.length];\n          \n          const circle = document.createElementNS('http://www.w3.org/2000/svg', 'circle');\n          circle.setAttribute('class', 'donut-segment');\n          circle.setAttribute('cx', '21');\n          circle.setAttribute('cy', '21');\n          circle.setAttribute('r', '15.9155');\n          circle.setAttribute('fill', 'transparent');\n          circle.setAttribute('stroke', sliceColor);\n          circle.setAttribute('stroke-width', '4');\n          \n          const strokeDashArray = `${percent} ${100 - percent}`;\n          const strokeDashOffset = -accumulatedPercent;\n          \n          circle.setAttribute('stroke-dasharray', strokeDashArray);\n          circle.setAttribute('stroke-dashoffset', strokeDashOffset.toString());\n          svg.appendChild(circle);\n          \n          accumulatedPercent += percent;\n          \n          const legendItem = document.createElement('div');\n          legendItem.style.display = 'flex';\n          legendItem.style.alignItems = 'center';\n          legendItem.style.justifyContent = 'space-between';\n          legendItem.style.width = '100%';\n          \n          legendItem.innerHTML = `\n            <div style=\"display: flex; align-items: center; gap: 8px;\">\n              <span style=\"width: 8px; height: 8px; border-radius: 50%; background-color: ${sliceColor}; display: inline-block;\"></span>\n              <span style=\"color: #c9d1d9; font-weight: 500;\">${slice.label}</span>\n            </div>\n            <span style=\"color: #8e8e93; font-weight: bold;\">${slice.value} (${percent.toFixed(0)}%)</span>\n          `;\n          legend.appendChild(legendItem);\n        });\n      })\n      .catch(err => {\n        console.error('Error updating donut chart:', err);\n      });\n  }\n  \n  updateDonut();\n  setInterval(updateDonut, 5000);\n})();\n</script>\n"
      }
    },
    {
      "title": "Request Throughput (req/s)",
      "type": "timeseries",
      "gridPos": {
        "x": 16,
        "y": 3,
        "w": 8,
        "h": 8
      },
      "id": 7,
      "datasource": "Prometheus",
      "targets": [
        {
          "datasource": "Prometheus",
          "expr": "sum(rate(api_requests_total{endpoint=\"/predict\"}[5m]))",
          "legendFormat": "POST /predict",
          "refId": "A"
        },
        {
          "datasource": "Prometheus",
          "expr": "sum(rate(api_requests_total{endpoint=\"/metrics\"}[5m]))",
          "legendFormat": "GET /metrics",
          "refId": "B"
        },
        {
          "datasource": "Prometheus",
          "expr": "sum(rate(api_requests_total{endpoint!~\"/predict|/metrics\"}[5m]))",
          "legendFormat": "Other",
          "refId": "C"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 2,
            "fillOpacity": 20,
            "gradientMode": "opacity",
            "spanNulls": false,
            "showPoints": "auto",
            "pointSize": 5,
            "stacking": {
              "mode": "normal",
              "group": "A"
            }
          },
          "color": {
            "mode": "palette-classic"
          }
        },
        "overrides": []
      },
      "options": {
        "tooltip": {
          "mode": "multi",
          "sort": "none"
        },
        "legend": {
          "showLegend": true,
          "displayMode": "list",
          "placement": "bottom"
        }
      }
    },
    {
      "title": "Prediction Class Distribution Timeline",
      "type": "timeseries",
      "gridPos": {
        "x": 0,
        "y": 11,
        "w": 8,
        "h": 8
      },
      "id": 14,
      "datasource": "Prometheus",
      "targets": [
        {
          "datasource": "Prometheus",
          "expr": "sum(rate(predictions_total{predicted_class=\"1\"}[5m])) * 60",
          "legendFormat": "Class 1",
          "refId": "A"
        },
        {
          "datasource": "Prometheus",
          "expr": "sum(rate(predictions_total{predicted_class=\"0\"}[5m])) * 60",
          "legendFormat": "Class 0",
          "refId": "B"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 2,
            "fillOpacity": 5,
            "gradientMode": "none",
            "spanNulls": false,
            "showPoints": "auto",
            "pointSize": 5,
            "stacking": {
              "mode": "none",
              "group": "A"
            }
          },
          "color": {
            "mode": "palette-classic"
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Class 0"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#3fb950"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Class 1"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#d29922"
                }
              }
            ]
          }
        ]
      },
      "options": {
        "tooltip": {
          "mode": "single",
          "sort": "none"
        },
        "legend": {
          "showLegend": true,
          "displayMode": "list",
          "placement": "bottom"
        }
      }
    },
    {
      "title": "HTTP Error Distribution Timeline",
      "type": "timeseries",
      "gridPos": {
        "x": 8,
        "y": 11,
        "w": 8,
        "h": 8
      },
      "id": 12,
      "datasource": "Prometheus",
      "targets": [
        {
          "datasource": "Prometheus",
          "expr": "sum(rate(api_requests_total{http_status=~\"4..|5..\"}[5m])) by (http_status) * 60",
          "legendFormat": "{{http_status}}",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 2,
            "fillOpacity": 20,
            "gradientMode": "opacity",
            "spanNulls": false,
            "showPoints": "auto",
            "pointSize": 5,
            "stacking": {
              "mode": "normal",
              "group": "A"
            }
          },
          "color": {
            "mode": "palette-classic"
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "400"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#1f6feb"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "401"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#bc8cff"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "404"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#58a6ff"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "422"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#d29922"
                }
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "500"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "mode": "fixed",
                  "fixedColor": "#f85149"
                }
              }
            ]
          }
        ]
      },
      "options": {
        "tooltip": {
          "mode": "multi",
          "sort": "none"
        },
        "legend": {
          "showLegend": true,
          "displayMode": "list",
          "placement": "bottom"
        }
      }
    },
    {
      "title": "Latency (ms)",
      "type": "timeseries",
      "gridPos": {
        "x": 16,
        "y": 11,
        "w": 8,
        "h": 8
      },
      "id": 8,
      "datasource": "Prometheus",
      "targets": [
        {
          "datasource": "Prometheus",
          "expr": "histogram_quantile(0.50, sum(rate(api_request_latency_seconds_bucket[5m])) by (le)) * 1000",
          "legendFormat": "p50",
          "refId": "A"
        },
        {
          "datasource": "Prometheus",
          "expr": "histogram_quantile(0.95, sum(rate(api_request_latency_seconds_bucket[5m])) by (le)) * 1000",
          "legendFormat": "p95",
          "refId": "B"
        },
        {
          "datasource": "Prometheus",
          "expr": "histogram_quantile(0.99, sum(rate(api_request_latency_seconds_bucket[5m])) by (le)) * 1000",
          "legendFormat": "p99",
          "refId": "C"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "custom": {
            "drawStyle": "line",
            "lineInterpolation": "smooth",
            "lineWidth": 2,
            "fillOpacity": 5,
            "gradientMode": "none",
            "spanNulls": false,
            "showPoints": "auto",
            "pointSize": 5,
            "stacking": {
              "mode": "none",
              "group": "A"
            }
          },
          "color": {
            "mode": "palette-classic"
          }
        },
        "overrides": []
      },
      "options": {
        "tooltip": {
          "mode": "single",
          "sort": "none"
        },
        "legend": {
          "showLegend": true,
          "displayMode": "list",
          "placement": "bottom"
        }
      }
    }
  ],
  "refresh": "5s",
  "schemaVersion": 36,
  "style": "dark",
  "tags": [],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-15m",
    "to": "now"
  },
  "timepicker": {},
  "timezone": "",
  "title": "AI Prediction Service",
  "uid": "hd-api-monitor",
  "version": 150
}